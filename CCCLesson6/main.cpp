#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;
struct Tree
{
    bool hasBeen;
    double time;
    int berry = 50;
};
int main()
{
    int arr[4] = {1,2,3,4};
    for(auto i: arr)
    {
        cout << i << " ";
    }
   /* #include<algorithm>
struct Tree
{
    bool hasBeen;
    double time;
    int berry = 50;
};
Tree compare(Tree t1, Tree t2)
{
	Tree t = (t1.time < t2.time) ? (t1:t2);
	return t;
}

int main()
{
	int day;
	cin >> day;
	int base;
	cin >> base;
	int d;
	cin >> d;
	vector <Tree> tree;
	bool been;
	double time;
	for(int i = 0; i>0;i++)
	{
		if(cin>> time)
		{
			cin >> been;
			if(num==1)
			{
				time/=2.0;
			}
							tree.emplace_back(Tree{visited,time});
		}
	}

	sort(Tree.begin(),Tree.end(),compare);
	for(int i = 1; i <= day; i++)
	{*/

    return 0;
}
